export const enum ConnectionName {
  DATALAKE = 'datalake',
  APPS = 'apps',
}

export const enum DatabaseName {
  DATALAKE_DB_NAME = 'datalake',
  APPS_DB_NAME = 'apps',
}

export const enum DatabaseConnection {
  HOST = 'DB_HOST',
  PORT = 'DB_PORT',
  USERNAME = 'DB_USER',
  PASSWORD = 'DB_PASSWORD',
}
